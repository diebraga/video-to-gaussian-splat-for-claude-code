.DS_Store

# Reconstruction data/output — regenerated locally, not checked in.
# example/ is intentionally kept and committed via Git LFS as a worked
# reference — see README.
my_house/
viewer/*.ply
viewer/*.splat

# Stray build artifacts, unrelated to this pipeline.
target/
